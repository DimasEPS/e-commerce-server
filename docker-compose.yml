version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: e-commerce-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped
